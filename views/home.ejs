<body class="container">
    <% include partials/header%>
        <h1>Home</h1>
        <p>
            <%=homeContent%>
        </p>
        
        <% for( let i = 0; i < posts.length; i++ ) { %>
            <h1><%= posts[i].title %></h1>
            <body>
                <%= posts[i].body %>
            </body>
        <% } %>

<% include partials/footer%>
</body>

</html>